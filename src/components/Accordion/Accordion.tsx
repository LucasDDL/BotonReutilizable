import { QuestionAnswer } from "../../../testData"

export const Accordion = ({data}: QuestionAnswer) => {
    return(
        <div>
            {data.map((item, index)=> (

            ))}
        </div>
    )
}